server:
  port: '8091'

spring:
  application:
    name: organization-service
  config:
    import: optional:configserver:http://localhost:8071

management:
  endpoints:
    web:
      exposure:
        include: '*'
